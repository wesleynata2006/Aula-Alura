# Aula-Alura
## oi

function setup() {
  createCanvas(400, 400);
  background("black");
  
}

function draw() {
  
  
  stroke("purple")
  fill("black")
  
  if (mouseIsPressed); {
  rect(mouseX, mouseY, 10, 15);
  }
}
